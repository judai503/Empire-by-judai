export const config = {
    sessions: 'EmpireSession',
    // Esta regex detecta el símbolo al inicio y permite flexibilidad
    prefix: /^[#!./-]/, 
    botName: 'Empire Bot',
    // Reemplaza con tu número real (con código de país y sin el +)
    owner: '50360438371', 
    // Texto que aparecerá al final de algunos mensajes
    marca: '🛡️ *E M P I R E  B O T*' 
};
